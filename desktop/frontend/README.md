# Interfaz de la DJ App

Frontend React/Vite servido por Flask dentro de pywebview. Contiene reproductor,
biblioteca, listas, deseos, configuración y panel de modo fiesta.

```powershell
npm ci
npm run lint
npm run build
```

El resultado `dist/` es un artefacto generado y no se versiona.
